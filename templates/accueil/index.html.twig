<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link  href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
<link rel="stylesheet" href={{ asset("assets/css/style.css") }}>
<link rel="stylesheet" href={{ asset("assets/img/jeux.jpg") }}>
</head>
<body>
    <div class="container my-4 ">
        <header class="section fixed-top bg-light" >
            <div class="container-fluid " style="background-color: #2e4255;">
                <div class="row align-items-center py-2">
                <nav class="navbar navbar-expand-md navbar-light  p-0">
                            
                    <div class="collapse navbar-collapse" id="navbarMenu1">
                    <div class="col-12 col-md-6 d-flex flex-wrap header-left">
                        <a class="me-3"><i class="bi bi-envelope-fill custom-icon"></i> contact@exemple.com</a>
                        <a class="me-3"><i class="bi bi-telephone-fill custom-icon"></i> +1 5589 55488 55</a>
                        <a  class="me-3"><i class="bi bi-geo-alt-fill custom-icon"></i> A108 Adam Street, NY</a>
                    </div>
                    <div class="col-12 col-md-6 text-md-end header-right d-flex justify-content-md-end justify-content-center flex-wrap mt-2 mt-md-0">
                        <a class="me-3"><i class="bi bi-twitter custom-icon"></i></a>
                        <a class="me-3"><i class="bi bi-facebook custom-icon"></i></a>
                        <a class="me-3"><i class="bi bi-instagram custom-icon"></i></a>
                        <a class="me-3"><i class="bi bi-skype custom-icon"></i></a>
                        <a class="me-3"><i class="bi bi-linkedin custom-icon"></i></a>
                    </div>
                </div>
                </div>
                </nav>
                <div class="row align-items-center py-2">
                    <div class="col-12 col-md-6" >
                        <a href="{{ path('app_afficheractualite')}}" class="logo h4 text-decoration-none"  style=" margin-left: 150px  ; color :  #FFFFFF" ><span class="bg-primary bg-gradient p-1 rounded-3 text-light">Jeux-</span>video.ci</a>
                    </div>
                    <div class="col-12 col-md-6 text-md-end">
                        <nav class="navbar navbar-expand-md navbar-light p-0">
                            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu"
                                aria-controls="navbarMenu" aria-expanded="false" aria-label="Toggle navigation">
                                <span class="navbar-toggler-icon"></span>
                            </button>
                            <div class="collapse navbar-collapse" id="navbarMenu" style = " margin-right: 100px ;color :  #FFFFFF"  >
                                <ul class="navbar-nav ms-auto" >
                              <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="actusDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false"  style = " color :  #FFFFFF">
                                    Actus
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="actusDropdown">
                                    <li><a class="dropdown-item" href="{{ path('app_afficheactualitevideo') }}"  id="lien1">Vidéos</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_afficheactualiteimage') }}"  id="lien1">Articles</a></li>
                                </ul>
                            </li>
                                    <li class="nav-item"><a class="nav-link" href="{{ path('app_afficherjeux') }}"  id="lien">Jeux</a></li>
                                    <li class="nav-item"><a class="nav-link" href="{{ path('app_affichertest') }}"  id="lien">Tests</a></li>
                                    <li class="nav-item"><a class="nav-link" href="{{ path('app_apercu') }}"  id="lien">Apercus</a></li>
                                    <li class="nav-item"><a class="nav-link" href="{{ path('app_affichervideo') }}"  id="lien">Multimédia</a></li>
                                    <li class="nav-item"><a class="nav-link" href="{{ path('app_afficherbonplan') }}" id="lien">Bons plans</a></li>
                                    <li class="nav-item">
                                        <a href="{{ path('app_appointement') }}" class="btn btn-primary"  style = " color :  #FFFFFF">Prendre un rendez-vous</a>
                                    </li>
                                </ul>
                            </div>
                        </nav>
                    </div>
                </div>
            </div>
        </header>
     <div class="py-2 py-md-3 py-lg-4">
        <!-- Section Xbox Series -->
        <section  py-2>
            <h2 class="jeu">Suivre jeuxvideo.com/Toute l'actu de la Xbox Series X et S et ses jeux</h2>
            
            <div class="row py-5">
            {% for jeu in jeux %}
                <div class="col-md-4 col-sm-6 py-2">
             
                    <div class="game-card" id="card">
                     
                        <span class="badge">NEW JEU</span>
                        <a href="{{ path('app_detaille', { 'id': jeu.id }) }}" class="game-link">  
                        <img src="{{ asset('uploads/images/' ~ jeu.imagePath) }}" alt="Anno 1800">
                        <p id="uni">{{ jeu.description|slice(0, 80) }}...
                   <span class="date">{{jeu.createAd|date('d/m/Y H:i') }}</span></p>
          </a>
                    </div>
        
                </div>
              {% else %}
        <p>Aucun jeu trouvé.</p>
    {% endfor %}
            </div>
         
        </section>

        <!-- Section Tendance -->
        <section>
            <h2 class="mt-0 mb-5">Tendance</h2>
            <div class="container">
            <div class="row">
             
                <div class="col-md-4">
                {% for jeu4 in jeux4 %}
                    <div class="game-card" id="card">
                        <span class="badge">News Jeu</span>
                        <img src="{{ asset('uploads/images/' ~ jeu4.imagePath) }}" alt="Hades">
                        <div class="card-body">
                                <p class="card-title">{{ jeu4.titre }} : (PS5,Xbox Series,Switch,PS4,Xbox <br> One,PC)</p>
                                <p class="act1">{{ jeu4.createAd|date('d/m/Y') }}</p>
                                <p class="card-text">{{ jeu4.description|slice(0, 100) }} ...</p>
                                
                             </div>
                    </div>
                
                    <div class="card mb-3" style="max-width: 450px; " id="card">
                        <div class="row g-0">
                          <div class="col-4">
                          
                            <img src="{{ asset('uploads/images/' ~ jeu4.imagePath) }}" class="img-fluid rounded-start" alt="..." style="width: 100%; height: 100%; object-fit: cover;">
                          </div>
                          <div class="col-8">
                            <div class="card-body">
                              <p class="card-text"> <span class="badge">News Jeu</span>
                              <span class="new">News Jeu :</span>{{ jeu4.titre }}  :{{ jeu4.description|slice(0, 40) }}...
                   <span class="date">{{jeu4.createAd|date('d/m/Y H:i') }}</span>
                            </div>
                          </div>
                        </div>
                      </div>
                </div>
                      
               {% else %}
        <p>Aucun jeu trouvé.</p>
    {% endfor %}
                 
                
                
                <div class="col-md-4">
                 {% for jeu5 in jeux5 %}
                    <div class="card mb-3" style="max-width: 450px; " id="card">
                        <div class="row g-0">
                          <div class="col-4">
                            <img src="{{ asset('uploads/images/' ~ jeu5.imagePath) }}" class="img-fluid rounded-start" alt="..." style="width: 100%; height: 100%; object-fit: cover;">
                          </div>
                          <div class="col-8">
                            <div class="card-body">
                              <p class="card-text"><span class="badge">News Jeu</span>
                              
                               <span class="new">News Jeu :</span> {{ jeu5.titre }} :{{ jeu5.description|slice(0, 40) }}...
                   <span class="date">{{jeu5.createAd|date('d/m/Y H:i') }}</span>
                              </p>
                            </div>
                          </div>
                        </div>
                      </div>
                       {% else %}
        <p>Aucun jeu trouvé.</p>
          {% endfor %}
                     
                      
                   </div>
                   <div class="col-md-4">
                    <div class="card">
                    <span class="fil">Fil d'actualité</span>
                      {% for actualiteimage1 in actualiteimage %}
                    <p class="act" id="card"> <span class="new">NEW :</span> {{ actualiteimage1.createAd|date('d/m/Y') }} {{ actualiteimage1.titre }} : {{ actualiteimage1.description|slice(0, 70) }}...</p>
                    {% endfor%}
                       </div>
                   </div>

            </div>
           </div>
        </section>
          <!-- Section Tendance -->
          <section>
            <h2 class="mt-5 mb-3">Promos et bons plans Xbox Series</h2>
            <div class="container">
            <div class="row">
            {% for vi in video %}
                <div class="col-md-4">
                
                 <div class="col-12 col-md-12" id="card">
                {% if vi.videoPath %}
                <video   style="width:100% ;height: 300px;" controls id="deo">
                    <source src="{{ asset('uploads/videos/' ~ vi.videoPath) }}" type="video/mp4">
                    Votre navigateur ne prend pas en charge la balise vidéo.
                </video>
            {% else %}
                <p>Aucune vidéo disponible.</p>
            {% endif %}
            <p id="uni1" class="card-text">{{ vi.description|slice(0, 100) }} ... du:{{vi.createAd|date('d/m/Y') }} </p>
            </div>
            
                </div>
                {% else %}
            <p>Aucune vidéo trouvée.</p>
            {% endfor %}
                
                
                   <div class="col-md-4">
                      {% for jeu2 in jeux2 %}
                    <div class="card mb-3" style="max-width: 450px; " id="card">
                        <div class="row g-0">
                          <div class="col-4" >
                            <img src="{{ asset('uploads/images/' ~ jeu2.imagePath) }}" class="img-fluid rounded-start" alt="..." style="width: 100%; height: 100%; object-fit: cover;" >
                          </div>
                          <div class="col-8">
                            <div class="card-body">
                              <p class="card-text"> <span class="badge">News Jeu</span> {{ jeu2.titre }}  :{{ jeu2.description|slice(0, 40) }}...
                   <span class="date">{{jeu2.createAd|date('d/m/Y H:i') }}</span> </p>
                            </div>
                          </div>
                        </div>
                      </div>
                       {% else %}
        <p>Aucun jeu trouvé.</p>
          {% endfor %}
                   </div>

            </div>
           </div>
        </section>
    </div>
  </div>

    <!-- Lien vers Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
<footer class="text-light py-4" style="background-color: #2e4255;">
    <div class="container">

        <div class="text-center mt-4">
            <p class="mb-0"> Copyright&copy; 2024 Jeux-vidéo.ci. Tous droits réservés.</p>
            <small class="d-block">Développé par GROUPE CIMEF.</small>
        </div>
    </div>
</footer>

</html>